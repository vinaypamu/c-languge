#include"head.h"
int sum(int x,int y)
{
	x=x+y;
	return x;
}
int sub(int x, int y)
{
	x=x-y;
	return x;
}
int mul(int x, int y)
{
	x=x*y;
	return x;
}
int div(int x, int y)
{
	x=x/y;
	return x;
}
int mod(int x, int y)
{
	x=x%y;
	return x;
}
